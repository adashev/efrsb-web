apply plugin: 'idea'
apply plugin: 'java'

configurations {
    agent
}

buildscript {
    repositories {
        jcenter()
    }
    dependencies {
        classpath 'com.bmuschko:gradle-nexus-plugin:2.3.1'
    }
}

subprojects {
    apply plugin: 'java'
    apply plugin: 'idea'
    apply plugin: 'maven'
    apply plugin: 'com.bmuschko.nexus'
    apply plugin: 'signing'

    archivesBaseName = baseArchiveName + '-' + project.name

    sourceCompatibility = 1.8

    repositories {
        mavenCentral()
    }

    repositories {
        maven {
            url "https://jitpack.io"
        }
    }

    dependencies {
        testCompile group: 'org.hamcrest', name: 'hamcrest-all', version: '1.3'
        testCompile group: 'commons-io', name: 'commons-io', version: '2.6'
    }

    modifyPom {
        project {
            name 'com.automation.remarks.video-recorder'
            packaging 'jar'
            description 'Video Recorder is a library that allows to record selenium tests'
            url 'https://github.com/SergeyPirogov/VideoRecorder'

            scm {
                url 'scm:git@github.com:SergeyPirogov/VideoRecorder.git'
                connection 'scm:git@github.com:SergeyPirogov/VideoRecorder.git'
                developerConnection 'scm:git@github.com:SergeyPirogov/VideoRecorder.git'
            }

            licenses {
                license {
                    name 'The Apache Software License, Version 2.0'
                    url 'http://www.apache.org/licenses/LICENSE-2.0.txt'
                    distribution 'repo'
                }
            }

            developers {
                developer {
                    id 'SerhiiP'
                    name 'Sergey Pirogov'
                }
            }
        }
    }

    extraArchive {
        sources = true
        tests = false
        javadoc = true
    }

    nexus {
        sign = true
    }

    test {
        testLogging {
            events "passed", "skipped", "failed"
            exceptionFormat = 'full'
        }
    }
}

repositories {
    mavenCentral()
}

dependencies {
    compile project(':testng')
    compile 'org.seleniumhq.selenium:selenium-java:3.9.1'
    agent "org.aspectj:aspectjweaver:1.8.10"
    implementation 'org.testng:testng:6.14.3'
    implementation 'io.qameta.allure:allure-testng:2.12.1'
    implementation 'org.slf4j:jcl-over-slf4j:1.7.28'
    implementation 'org.slf4j:slf4j-api:1.7.28'
    implementation 'com.google.code.gson:gson:2.8.6'
}

task testMessagesAU(type: Test) {
    useTestNG(){
        suites 'src/test/resources/testng-messageSign-AU.xml'
    }
    if (project.hasProperty('browser')){
        systemProperty 'browser', "${browser}"
    }
    if (project.hasProperty('target')){
        systemProperty 'target', "${target}"
    }
    if (project.hasProperty('contur')){
        systemProperty 'contur', "${contur}"
    }
    if (project.hasProperty('role')){
        systemProperty 'role', "${role}"
    }
    systemProperty 'allure.results.directory', 'build/allure-results'
}

testMessagesAU.doFirst {  jvmArgs "-javaagent:${configurations.agent.singleFile}" }

task testMessages_OTUR(type: Test) {
    useTestNG(){
        suites 'src/test/resources/testng-messageSign-OTUR.xml'
    }
    if (project.hasProperty('browser')){
        systemProperty 'browser', "${browser}"
    }
    if (project.hasProperty('target')){
        systemProperty 'target', "${target}"
    }
    if (project.hasProperty('contur')){
        systemProperty 'contur', "${contur}"
    }
    if (project.hasProperty('role')){
        systemProperty 'role', "${role}"
    }
    systemProperty 'allure.results.directory', 'build/allure-results'
}

testMessages_OTUR.doFirst {  jvmArgs "-javaagent:${configurations.agent.singleFile}" }

tasks.withType(JavaCompile) {
    options.encoding = 'UTF-8'
}









